import ReactModal from './ReactModal';

const Counter = (props) => {
  return (
    <ReactModal {...props}>
      <p>Please go to the counter to pay</p>
    </ReactModal>
  );
};
export default Counter;
